FROM node:22-alpine as base
WORKDIR /app/frontend
COPY package*.json ./
RUN npm install

FROM base AS dev
EXPOSE 5173
CMD ["npm", "run", "dev"]

# FROM base AS build
# RUN npm run build

FROM base as prod
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev"]