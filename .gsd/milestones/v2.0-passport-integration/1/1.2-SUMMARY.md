# Plan 1.2: Shared Validation Contracts, Server Model Utils & Unit Tests — Summary

## Completed: 2026-09-06

### What was done

1. **Universal PassPort Schemas (`shared/models/passport.ts`)**:
   - `passPortRegistrationStatusSchema`: enum validation for `NOT_REGISTERED`, `PENDING`, `REGISTERED`, `FAILED`.
   - `passPortPhase1RequestSchema`: validates Broker registration request payload with valid URLs and `passport_version: '1.0'`.
   - `passPortPhase2CredentialsSchema`: validates tool credentials delivery payload with `endpoints.extension_handler`, `credentials.client_id`, `credentials.client_secret`, and `requested_properties`.
   - `passPortExtensionPayloadSchema`: validates Section 2.B extension request payload (`request_id` uuid, `context`, `user`, `resource`, `extension`).
   - `passPortRollbackPayloadSchema`: validates Section 5 rollback payload with `request_id` uuid.
2. **Updated Tool Models (`shared/models/tool.ts`)**:
   - Extended `toolRowSchema` with `supportsProxy`, `supportsPassport`, `passportClientId`, `passportRegistrationUrl`, `passportExtensionUrl`, `passportRegistrationStatus`, `passportRegistrationError`, `passportRegisteredAt`, and `passportRequestedProperties`.
   - Updated `createToolSchema`, `updateToolSchema`, and `initialToolState` with appropriate defaults (`supportsProxy: true`, `supportsPassport: false`, etc.) and safe sanitization.
3. **Updated Server Tool CRUD (`server/utils/lti-tools.ts`)**:
   - Centralized projection via `toToolRow` to ensure consistent date formatting, property array preservation, and avoiding leaking sensitive secrets to standard projections.
   - Added `sanitizeToolData` to convert empty string URLs to `null`.
4. **Comprehensive Vitest Unit Tests**:
   - `test/unit/shared/models/passport.test.ts`: 10 tests covering registration status, Phase 1 & 2 schemas, extension payloads, and rollback delete payloads.
   - `test/unit/shared/models/tool.test.ts`: 4 tests covering row schema validation, default creation flags, and initial state.
   - `test/unit/server/utils/lti-tools.test.ts`: 4 tests verifying `getAllTools`, `getTool`, `createTool`, and `updateTool` with PassPort fields and URL sanitization.
   - Full suite passing: 88 test files, 409 tests passed with 0 failures.

### Verification Evidence

- `docker compose exec -T app-dev pnpm test:unit test/unit/shared/models/passport.test.ts test/unit/shared/models/tool.test.ts test/unit/server/utils/lti-tools.test.ts` exited with code 0 (18/18 tests passed).
- Full unit test suite `docker compose exec -T app-dev pnpm test:unit` passed all 409 tests.
