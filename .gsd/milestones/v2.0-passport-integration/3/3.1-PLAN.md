---
phase: 3
plan: 1
wave: 1
depends_on: []
files_modified:
  - app/composables/features/admin/useAdminTools.ts
  - test/unit/app/composables/useAdminTools.spec.ts
  - app/pages/admin/tools.vue
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'useAdminTools exposes registerPassPort calling POST /api/admin/tools/:id/passport/register'
    - 'app/pages/admin/tools.vue displays roles (Proxy and PassPort) and live PassPort registration status badges'
    - 'Table row actions allow triggering PassPort registration with instant toast notification'
    - '100% test pass rate for useAdminTools composable'
  artifacts:
    - app/composables/features/admin/useAdminTools.ts
    - test/unit/app/composables/useAdminTools.spec.ts
    - app/pages/admin/tools.vue
---

# Plan 3.1: Composable Extension & Admin Tool Table Status Displays

<objective>
Enhance `useAdminTools` composable with PassPort registration capability, test it with Vitest, and update the Admin Tools table view (`app/pages/admin/tools.vue`) to show dual-role indicators, registration status badges, error details, and a quick-action registration trigger.

Purpose: Provide administrators with clear visibility into tool capabilities and registration statuses across all configured LTI tools.
Output: Updated `useAdminTools.ts`, unit test suite `useAdminTools.spec.ts`, and updated `app/pages/admin/tools.vue`.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- .gsd/DECISIONS.md
- app/composables/features/admin/useAdminTools.ts
- app/pages/admin/tools.vue
- shared/models/tool.ts
- shared/models/passport.ts
</context>

<tasks>

<task type="auto">
  <name>Extend useAdminTools composable with registerPassPort and write unit tests</name>
  <files>app/composables/features/admin/useAdminTools.ts, test/unit/app/composables/useAdminTools.spec.ts</files>
  <action>
    1. Update `app/composables/features/admin/useAdminTools.ts`:
       - Add `registerPassPort: (id: string) => Promise<ApiResponse<ToolRow>>`.
       - Dispatches `POST /api/admin/tools/${id}/passport/register`.
       - Expose `registerPassPort` in return object.
    2. Create `test/unit/app/composables/useAdminTools.spec.ts`:
       - Test `fetchTools`: calls useFetch with `/api/admin/tools`.
       - Test `saveTool`: calls $fetch with POST for new tool and PATCH for existing tool.
       - Test `deleteTool`: calls $fetch with DELETE.
       - Test `registerPassPort`: calls $fetch with POST `/api/admin/tools/${id}/passport/register`.
  </action>
  <verify>docker compose exec -T app-dev pnpm test:unit test/unit/app/composables/useAdminTools.spec.ts</verify>
  <done>registerPassPort function implemented and verified with Vitest.</done>
</task>

<task type="auto">
  <name>Update Admin Tools table to display roles, PassPort status badges, and registration actions</name>
  <files>app/pages/admin/tools.vue</files>
  <action>
    Update `app/pages/admin/tools.vue`:
    1. Add Roles Column in `toolColumns`:
       - Shows badge for "Proxy" if `supportsProxy` is true (subtle neutral).
       - Shows badge for "PassPort" if `supportsPassport` is true (subtle primary).
    2. Add PassPort Status Column in `toolColumns`:
       - If `!supportsPassport`: render '—'.
       - If `supportsPassport`: render status badge:
         - `REGISTERED`: color 'success', icon 'i-lucide-check-circle', label 'Registered'.
         - `PENDING`: color 'info', icon 'i-lucide-clock', label 'Pending'.
         - `FAILED`: color 'error', icon 'i-lucide-alert-circle', label 'Failed' (with error text in title/tooltip if `passportRegistrationError`).
         - `NOT_REGISTERED`: color 'neutral', icon 'i-lucide-circle-dashed', label 'Not Registered'.
    3. Update Row Actions Menu:
       - If `supportsPassport`, add action "Register with PassPort" (icon 'i-lucide-send').
       - Clicking action calls `registerPassPort(row.id)`.
       - Shows loading state or success toast "PassPort registration initiated", then refreshes table via `onItemCreated()`.
       - Handles error: shows error toast with error message.
  </action>
  <verify>grep -q "registerPassPort" app/pages/admin/tools.vue && grep -q "supportsPassport" app/pages/admin/tools.vue</verify>
  <done>Admin tools table displays role indicators, status badges, and registration action.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] `useAdminTools` exports `registerPassPort`
- [ ] `test/unit/app/composables/useAdminTools.spec.ts` passes
- [ ] `app/pages/admin/tools.vue` contains roles, status badges, and registration action
</verification>

<success_criteria>

- [ ] All tasks verified
- [ ] Must-haves confirmed
      </success_criteria>
